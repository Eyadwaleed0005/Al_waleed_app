import 'package:al_waleed/features/auth/presentation/screens/widgets/auth_view_body.dart';
import 'package:flutter/material.dart';

class AuthView extends StatelessWidget {
  const AuthView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const AuthViewBody());
  }
}
